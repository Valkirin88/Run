# 🎮 Простая глобальная многопользовательская игра

Упрощенная система для подключения 4 игроков из любой точки мира через Unity Relay.

## 🎯 Логика работы

### Простая схема "первый = хост":
```
1. Игрок нажимает "Старт"
2. Ищет существующих хостов (2 сек)
3. Если не найдет → становится ХОСТОМ
4. Если найдет → подключается как КЛИЕНТ
5. Максимум 4 игрока в одной игре
```

## 📁 Файлы системы

### 1. `SimpleRelayManager.cs` - Основной менеджер
- Наследуется от Mirror NetworkManager
- Автоматическая логика хост/клиент
- Интеграция с Unity Relay
- Максимум 4 игрока

### 2. `SimpleGameUI.cs` - Простой UI
- Кнопка "Старт" 
- Отображение статуса
- Код игры для друзей
- Счетчик игроков

## ⚙️ Настройка в Unity

### 1. Установить пакеты:
```
Package Manager → Unity Registry:
- Unity Relay
- Unity Authentication  
- Unity Services Core
```

### 2. Unity Services:
```
Edit → Project Settings → Services:
- Создать проект в Unity Dashboard
- Активировать Relay Service
- Скопировать Project ID
```

### 3. Создать GameObject "NetworkManager":
```
Компоненты:
- SimpleRelayManager (Script)
- kcp2k Transport (Mirror транспорт)
```

### 4. Настроить UI:
```
Canvas:
├── LobbyPanel
│   ├── StartButton → SimpleRelayManager.OnStartButtonClick()
│   ├── StatusText
│   ├── JoinCodeInput
│   └── JoinButton → SimpleRelayManager.JoinWithCode()
├── GamePanel  
│   ├── PlayersCountText
│   ├── JoinCodeText
│   └── CopyCodeButton
└── SimpleGameUI (Script)
```

## 🎮 Как играть

### Создать игру:
```
1. Игрок 1: Нажимает "Старт"
2. Становится хостом
3. Получает код (например: ABC123)
4. Делится кодом с друзьями
```

### Присоединиться:
```
1. Игрок 2-4: Вводят код ABC123
2. Нажимают "Присоединиться"  
3. Подключаются к игре
4. Игра автоматически начинается
```

### Автопоиск:
```
1. Любой игрок: Нажимает "Старт"
2. Система автоматически:
   - Ищет существующие игры
   - Подключается к найденной
   - Или создает новую
```

## 📱 Сборка APK

### Build Settings:
```
Platform: Android
- Minimum API: 21
- Target API: 30+
- Scripting Backend: IL2CPP
```

### Player Settings:
```
Internet Access: Require
Permissions:
✅ Internet
✅ Access Network State
```

### Project Settings:
```
Services:
✅ Initialize Unity Services on Startup
✅ Project ID: [ваш ID]
```

## 🔧 Код для интеграции

### Инициализация:
```csharp
// В сцене должен быть GameObject с SimpleRelayManager
var relayManager = FindObjectOfType<SimpleRelayManager>();
```

### Кнопка "Старт":
```csharp
public Button startButton;

void Start() {
    startButton.onClick.AddListener(() => {
        FindObjectOfType<SimpleRelayManager>().OnStartButtonClick();
    });
}
```

### Присоединение по коду:
```csharp
public InputField joinCodeInput;

public void JoinGame() {
    string code = joinCodeInput.text;
    FindObjectOfType<SimpleRelayManager>().JoinWithCode();
}
```

## 🌍 Результат

После настройки ваша игра поддерживает:

✅ **Глобальные подключения** - игроки из разных стран  
✅ **Простой код игры** - 6 символов типа "ABC123"  
✅ **Автоматический хост** - первый игрок становится хостом  
✅ **До 4 игроков** в одной игре  
✅ **Работа на Android/PC** из коробки  
✅ **Бесплатно** до 100 одновременных игр  

## 💡 Простота использования

Пользователи просто:
1. Запускают игру
2. Нажимают "Старт" 
3. Система сама решает кто хост, кто клиент
4. Делятся кодом с друзьями
5. Играют вместе из любой точки мира!

**Никаких настроек сети, портов, IP адресов!** 🎉
