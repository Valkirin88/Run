# 🎮 Тестирование в Unity Editor Multiplayer Mode

## 🐛 Проблема:
В Unity Multiplayer Play Mode каждый экземпляр создавал свой Relay allocation и не видел другие экземпляры.

## ✅ Решение:
Добавлена симуляция matchmaking для Unity Editor с shared состоянием между экземплярами.

## 🔧 Как это работает:

### В Unity Editor:
```csharp
// Статические переменные shared между экземплярами
private static string sharedJoinCode = "";
private static bool hasActiveHost = false;
```

### Логика для Editor:
1. **Первый экземпляр:** Создает хост → сохраняет join код в static переменную
2. **Второй экземпляр:** Видит активный хост → подключается как клиент по shared join коду

### В продакшене (APK):
- Используется обычный Unity Relay matchmaking
- Static переменные не влияют (разные процессы)

## 🧪 Пошаговое тестирование:

### 1. Настройка Unity Editor:
```
Window → General → Multiplayer Play Mode
- Enable Multiplayer Play Mode
- Player Count: 2
- Main Camera Tag: MainCamera
```

### 2. Запуск тестирования:
```
1. Нажать Play в Multiplayer Mode
2. В первом окне нажать "Старт" 
   → Должен стать хостом: "Ожидание 2-го игрока... (1/2)"
3. Во втором окне нажать "Старт"
   → Должен подключиться как клиент: "Подключен к игре"
4. Автостарт: "Игра началась! Оба игрока подключились!"
```

### 3. Что должно происходить:

#### Первый экземпляр (Хост):
```
- Поиск игры... (3 сек)
- Создание игры...
- Ожидание 2-го игрока... (1/2)
- [При подключении 2-го] Игра началась! Оба игрока подключились!
```

#### Второй экземпляр (Клиент):
```
- Поиск игры...
- Найден активный хост в Editor, подключаемся как клиент
- Подключение к игре...
- Подключен к игре
- Игра началась! Оба игрока подключились!
```

## 📝 Debug логи для проверки:

### Должны появиться в Console:
```
Editor: Сохранен join код для других экземпляров: ABC123
Найден активный хост в Editor, подключаемся как клиент
Подключение к игре с кодом: ABC123
```

## ⚠️ Важные моменты:

### 1. Только для Unity Editor:
```csharp
#if UNITY_EDITOR
// Код симуляции matchmaking
#endif
```

### 2. В APK сборке:
- Код симуляции отключается
- Работает обычный Unity Relay
- Каждое устройство - отдельный процесс

### 3. Если что-то не работает:
- Остановить все экземпляры
- Подождать 2-3 секунды
- Запустить заново

## 🚀 Результат:

✅ **Работает в Unity Editor** - два экземпляра видят друг друга  
✅ **Первый становится хостом** - автоматически  
✅ **Второй подключается как клиент** - автоматически  
✅ **Автостарт при 2 игроках** - игра начинается сама  
✅ **Готово для APK** - в продакшене работает через Relay  

## 🎯 Тестирование реального matchmaking:

Для тестирования настоящего Unity Relay:
1. Соберите APK
2. Установите на 2 устройства
3. Запустите на обоих
4. Нажмите "Старт" на каждом
5. Система автоматически найдет/создаст игру

**Теперь можно тестировать локально в Unity Editor!** 🎉
