# 🧪 Инструкции по тестированию matchmaking

## 🔍 Проблема: игроки не видят друг друга

Возможные причины:
1. Unity Relay требует специальной настройки
2. Static переменные не работают как ожидается
3. Конфликт портов между экземплярами

## 🎯 Решение: два способа тестирования

### Способ 1: Отладка Unity Relay (с логами)

Использует `SimpleRelayManager` с дополнительными логами:

#### Что смотреть в Console:
```
🎮 ХОСТ СОЗДАН! Сохранен join код: ABC123
🔥 hasActiveHost = True, sharedJoinCode = 'ABC123'

// При запуске второго экземпляра:
🎯 НАЙДЕН АКТИВНЫЙ ХОСТ! Join код: ABC123
```

#### Если НЕ работает Unity Relay:
```
🔍 НЕТ АКТИВНОГО ХОСТА. hasActiveHost: False, sharedJoinCode: ''
```

### Способ 2: Локальное тестирование (без Relay)

Использует `LocalTestManager` - простой локальный NetworkManager:

#### Настройка:
1. Замените `SimpleRelayManager` на `LocalTestManager` в сцене
2. Подключите те же UI элементы
3. Запустите Multiplayer Play Mode

#### Что должно произойти:
```
🚀 КНОПКА СТАРТ НАЖАТА В ЛОКАЛЬНОМ РЕЖИМЕ!
🎮 СОЗДАЕМ ХОСТ НА ПОРТУ: 7777
🎉 ЛОКАЛЬНЫЙ ХОСТ СОЗДАН УСПЕШНО!

// Второй экземпляр:
🎯 НАЙДЕН ЛОКАЛЬНЫЙ ХОСТ: localhost
🔌 ПОДКЛЮЧАЕМСЯ К ХОСТУ: localhost:7777
🎯 КЛИЕНТ ПОДКЛЮЧЕН К ХОСТУ
🎉 ИГРА НАЧАЛАСЬ - ОБА ИГРОКА В СБОРЕ!
```

## 🔧 Пошаговое тестирование

### Тест 1: Unity Relay (текущий код)
```
1. Убедитесь что используется SimpleRelayManager
2. Запустите Multiplayer Play Mode (2 игрока)
3. В первом окне нажмите "Старт"
4. Проверьте Console - должен появиться "🎮 ХОСТ СОЗДАН!"
5. Во втором окне нажмите "Старт"
6. Проверьте Console - должен появиться "🎯 НАЙДЕН АКТИВНЫЙ ХОСТ!"
```

### Тест 2: Локальная сеть (если Relay не работает)
```
1. Замените SimpleRelayManager на LocalTestManager в сцене
2. Подключите UI элементы к LocalTestManager
3. Запустите Multiplayer Play Mode (2 игрока)
4. В первом окне нажмите "Старт" - должен стать хостом
5. Во втором окне нажмите "Старт" - должен подключиться как клиент
```

## 🐛 Диагностика проблем

### Если static переменные не работают:
```csharp
// Добавьте в Start() метод:
Debug.Log($"СТАРТ: hasActiveHost = {hasActiveHost}");

// Добавьте в OnStartButtonClick():
Debug.Log($"КНОПКА: hasActiveHost = {hasActiveHost}");
```

### Если порты заняты:
```
1. Остановите все экземпляры
2. Подождите 5 секунд
3. Запустите заново
4. Или используйте LocalTestManager (автоматически решает конфликты)
```

### Если Unity Services не инициализированы:
```
Project Settings → Services:
- Project ID должен быть установлен
- Initialize Unity Services on Startup = ✅
```

## 🎯 Быстрое решение

**Рекомендация:** Используйте `LocalTestManager` для первичного тестирования matchmaking логики.

Это исключит проблемы с Unity Relay и покажет работает ли базовая система "первый = хост, второй = клиент".

После успешного локального тестирования можно вернуться к отладке Unity Relay.

## 📋 Checklist

- [ ] Console показывает логи подключения
- [ ] Первый экземпляр становится хостом  
- [ ] Второй экземпляр подключается как клиент
- [ ] UI обновляется корректно
- [ ] Счетчик игроков работает
- [ ] Автостарт при 2 игроках

**Выберите подходящий способ тестирования и следуйте инструкциям!** 🚀
